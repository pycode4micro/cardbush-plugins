# 交付验证

验证日期：2026-09-16。运行环境：Windows、Python 3.12，MCP Python SDK 2.2.0。

| 检查 | 结果 |
| --- | --- |
| 自动化测试 | **74 passed**，包含单元、模拟 HTTP、真实 stdio 子进程和迁移启动测试 |
| Ruff 静态检查 | 通过，覆盖 src、tests、scripts |
| OpenAI 兼容插件校验器 | 通过 |
| 技能 frontmatter 校验 | 通过 |
| 根 plugin.json | 通过 Agent Plugins 1.0.0 官方 JSON Schema |
| 根 mcp.json 和写入配置示例 | 通过对应官方 JSON Schema |
| 默认工具集 | 14 个；远程修改工具未注册 |
| 读写均开启 | 28 个 |
| 两种能力均关闭 | 仅保留不联网的状态工具 |
| 启动参数覆盖环境变量 | 开启与关闭两个方向均通过 |
| 隐藏工具和 HTTP 层写入拦截 | 通过，拒绝前不发送鉴权请求 |
| POST 形式的多维表格搜索 | 只读模式可查询；关闭读取时被拦截 |
| 跨目录迁移 | 复制到含空格的新目录，按便携 MCP 清单启动；依赖在外部数据目录安装，无原业务项目依赖 |

真实飞书只读验证：使用参考工作流的现有应用身份，通过标准 MCP 清单启动插件，读取机器人信息、电子表格元数据及全部 6 个页签。每页抽查 A1:L20，共 120 行，与原客户端逐格一致；5 个账号的页签匹配只读预览通过。修改工具未注册，真实修改调用次数为 0。凭据只通过测试子进程环境临时传入；插件和仓库不包含真实凭据、表格 token 或抽样单元格数据。

写入验证采用模拟 HTTP，包括消息 content 序列化与 UUID、webhook 签名、表格追加 INSERT_ROWS、范围和批次限制、账号路由歧义、创建/扩容，以及部分失败后的进度反馈。没有对真实飞书发送消息、新建页签或修改数据，因此不声称已经验证生产环境的写入权限。多维表格和消息列表的实际可访问范围还取决于使用者自己的飞书授权。

测试可通过 `uv run --frozen pytest -q` 复现。测试不依赖真实飞书凭据；迁移启动测试需要 uv，并可能在首次运行时下载锁定的依赖。
